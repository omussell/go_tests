load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    prefix = "github.com/omussell/go_tests",
)

gazelle(
    name = "gazelle-gofakeit",
    prefix = "github.com/brianvoe/gofakeit",
)
